Use this to get input from a user:
console.log(process.argv[i]);

to export you need
module.exports = {
  essentials: essentials,
  niceToHaves: niceToHaves
};

to import use requre
var bandList = require("./bands.js");